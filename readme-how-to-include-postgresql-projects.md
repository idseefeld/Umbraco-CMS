# How to include the PostgreSQL projects
I have put the PostgreSQL related projects into there own GitHub repository to keep code, issue tracker and wiki pages together. 
This repository is linked as a Git submodule. In case the submodule is not yet initialized, you can do so with the following command:
```
git submodule update --init --recursive
```
